---
apiVersion: v1
kind: Service
metadata:
  name: netbox-mcp-server
  namespace: netbox
  labels:
    app.kubernetes.io/name: netbox-mcp-server
    app.kubernetes.io/part-of: netbox
    home-lab.io/project-slug: home-automation
spec:
  selector:
    app.kubernetes.io/name: netbox-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
