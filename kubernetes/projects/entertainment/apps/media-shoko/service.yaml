apiVersion: v1
kind: Service
metadata:
  name: shoko
  namespace: media
  labels:
    app.kubernetes.io/name: shoko
    app.kubernetes.io/part-of: media-stack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: shoko
  ports:
    - name: http
      port: 8111
      targetPort: http
      protocol: TCP
