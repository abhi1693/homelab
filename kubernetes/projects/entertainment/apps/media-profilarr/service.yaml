---
apiVersion: v1
kind: Service
metadata:
  name: profilarr
  namespace: media
  labels:
    app.kubernetes.io/name: profilarr
    app.kubernetes.io/part-of: media-stack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: profilarr
  ports:
    - name: http
      port: 6868
      targetPort: http
      protocol: TCP
