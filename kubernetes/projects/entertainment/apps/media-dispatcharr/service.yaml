apiVersion: v1
kind: Service
metadata:
  name: dispatcharr
  namespace: media
  labels:
    app.kubernetes.io/name: dispatcharr
    app.kubernetes.io/part-of: media-stack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dispatcharr
  ports:
    - name: http
      port: 9191
      targetPort: http
      protocol: TCP

