apiVersion: apps/v1
kind: Deployment
metadata:
  name: wardn-hub-review-webhook
  namespace: wardn
  labels:
    app.kubernetes.io/name: wardn-hub-review-webhook
    app.kubernetes.io/component: review-webhook
    app.kubernetes.io/part-of: wardn-hub
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: wardn-hub-review-webhook
      app.kubernetes.io/component: review-webhook
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wardn-hub-review-webhook
        app.kubernetes.io/component: review-webhook
        app.kubernetes.io/part-of: wardn-hub
        wardn-hub-api-client: "true"
        wardn-hub-api-rollout-waiter: "true"
        wardn-hub-valkey-client: "true"
    spec:
      automountServiceAccountToken: false
      serviceAccountName: wardn-hub-api-rollout-waiter
      securityContext:
        fsGroup: 999
        fsGroupChangePolicy: OnRootMismatch
      imagePullSecrets:
        - name: harbor-registry
      nodeSelector:
        kubernetes.io/arch: arm64
      initContainers:
        - name: wait-for-api-rollout
          # renovate: datasource=docker depName=ghcr.io/abhi1693/wardn-hub-backend
          image: registry.home/ghcr.io/abhi1693/wardn-hub-backend:0.2.66
          imagePullPolicy: IfNotPresent
          command:
            - python
            - /opt/wardn/wait-for-api-rollout.py
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          volumeMounts:
            - name: api-rollout-script
              mountPath: /opt/wardn/wait-for-api-rollout.py
              subPath: wait-for-api-rollout.py
              readOnly: true
            - name: api-rollout-token
              mountPath: /var/run/secrets/wardn-hub-api-rollout
              readOnly: true
        - name: wait-for-codex-login
          # renovate: datasource=docker depName=ghcr.io/abhi1693/wardn-hub-backend
          image: registry.home/ghcr.io/abhi1693/wardn-hub-backend:0.2.66
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -ec
          args:
            - |
              for attempt in $(seq 1 180); do
                if codex login status; then
                  exit 0
                fi
                echo "Waiting for shared Codex login in /app/.codex (${attempt}/180)..."
                sleep 10
              done
              echo "Timed out waiting for shared Codex login."
              exit 1
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          volumeMounts:
            - name: codex-config
              mountPath: /app/.codex
        - name: sync-event-rule
          # renovate: datasource=docker depName=ghcr.io/abhi1693/wardn-hub-backend
          image: registry.home/ghcr.io/abhi1693/wardn-hub-backend:0.2.66
          imagePullPolicy: IfNotPresent
          command:
            - python
            - -c
          args:
            - |
              import os
              import sys
              import time

              import httpx

              name = "Submission review webhook"
              base_url = os.environ["WARDN_HUB_API_INTERNAL_BASE_URL"].rstrip("/")
              webhook_url = os.environ["WARDN_HUB_REVIEW_WEBHOOK_URL"]
              token = os.environ["WARDN_HUB_TOKEN"]
              signing_secret = os.environ["WARDN_HUB_REVIEW_WEBHOOK_SECRET"]
              headers = {"Authorization": f"Bearer {token}"}
              payload = {
                  "name": name,
                  "description": "Automatically reviews submitted MCP server definitions.",
                  "isEnabled": True,
                  "eventTypes": ["submission.submitted"],
                  "conditions": {},
                  "actionType": "webhook",
                  "actionConfig": {
                      "url": webhook_url,
                      "signingSecret": signing_secret,
                  },
                  "failurePolicy": {},
              }

              last_error = None
              for _attempt in range(24):
                  try:
                      with httpx.Client(timeout=30) as client:
                          response = client.get(f"{base_url}/api/v1/events/rules", headers=headers)
                          response.raise_for_status()
                          rules = response.json().get("rules", [])
                          existing = next((rule for rule in rules if rule.get("name") == name), None)
                          if existing:
                              response = client.put(
                                  f"{base_url}/api/v1/events/rules/{existing['id']}",
                                  headers=headers,
                                  json=payload,
                              )
                          else:
                              response = client.post(
                                  f"{base_url}/api/v1/events/rules",
                                  headers=headers,
                                  json=payload,
                              )
                          response.raise_for_status()
                          print(f"Synced event rule: {name}", flush=True)
                          sys.exit(0)
                  except Exception as exc:
                      last_error = exc
                      print(f"Waiting to sync event rule: {exc}", file=sys.stderr, flush=True)
                      time.sleep(5)
              raise SystemExit(f"Failed to sync event rule: {last_error}")
          envFrom:
            - configMapRef:
                name: wardn-hub
            - secretRef:
                name: wardn-hub-review-webhook
          env:
            - name: WARDN_HUB_REVIEW_WEBHOOK_URL
              value: http://wardn-hub-review-webhook.wardn.svc.cluster.local:8090/webhooks/wardn/submission-review
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
      volumes:
        - name: api-rollout-script
          configMap:
            name: wardn-hub-api-rollout-waiter
            defaultMode: 0444
        - name: api-rollout-token
          projected:
            defaultMode: 0444
            sources:
              - serviceAccountToken:
                  path: token
                  expirationSeconds: 1800
              - configMap:
                  name: kube-root-ca.crt
                  items:
                    - key: ca.crt
                      path: ca.crt
        - name: codex-config
          persistentVolumeClaim:
            claimName: wardn-hub-codex-config
      containers:
        - name: review-webhook
          # renovate: datasource=docker depName=ghcr.io/abhi1693/wardn-hub-backend
          image: registry.home/ghcr.io/abhi1693/wardn-hub-backend:0.2.66
          imagePullPolicy: IfNotPresent
          command:
            - python
            - -m
            - app.cli.review_submission_webhook
          args:
            - --host
            - 0.0.0.0
            - --port
            - "8090"
            - --url
            - http://wardn-hub-api.wardn.svc.cluster.local:8080
            - --review-timeout
            - "1200"
            - --review-progress-interval
            - "30"
            - --auto-publish
            - --verbose
          envFrom:
            - configMapRef:
                name: wardn-hub
            - secretRef:
                name: wardn-hub-review-webhook
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: WARDN_HUB_OTEL_SERVICE_NAME
              value: wardn-hub-review-webhook
            - name: WARDN_HUB_OTEL_RESOURCE_ATTRIBUTES
              value: service.namespace=wardn-hub,deployment.environment.name=production,service.instance.id=$(POD_NAME),k8s.namespace.name=wardn,k8s.deployment.name=wardn-hub-review-webhook,k8s.pod.name=$(POD_NAME),k8s.container.name=review-webhook
          ports:
            - name: http
              containerPort: 8090
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 512Mi
            limits:
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /health/ready
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 60
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 4
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          volumeMounts:
            - name: codex-config
              mountPath: /app/.codex
