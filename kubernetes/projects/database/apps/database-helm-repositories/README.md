# Database Helm Repositories

This bundle registers Rancher `ClusterRepo` resources used by shared database
infrastructure.

It owns:

- `oci://registry-1.docker.io/bitnamicharts/valkey`
- `https://cloudnative-pg.github.io/charts`
